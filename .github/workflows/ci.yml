name: CI
on:
  pull_request:
    branches: [dev, master]
  push:
    branches: [dev]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm run build  # oder dein Build-Skript neu auch neu